# 验证记录

## 最佳局部重分配模型（2026-09-21）

本节是当前页面的验证；下方日期较早的记录仅用于历史追溯。

- 模型为 `reallocated_latent_s2042`，实际载荷 259,912 字节，描述 1,759 字节，共 261,671 字节。
- 最新重建 PNG 独立计算 PSNR 46.048776 dB；搜索前 PNG 从同次实验的 `qat_best.pt` 渲染，PSNR 44.342115 dB，与第 0 轮记录一致。15 轮搜索终点与最新 PNG 指标一致。
- `node verify.mjs` 通过：6 个运行资产的 SHA-256、网格与网络维度、两级插值权重、实际字节数、历史端点均检查。
- 12 个固定参考像素（包含四角）＋256 个固定随机像素通过。JavaScript 与 PyTorch 最大 RGB 浮点绝对误差为 `2.00960406093742e-7`；每通道 RGB8 差不超过 1。
- 中英文静态页面已重新生成，英文静态内容翻译覆盖检查通过；`node --check app.mjs` 通过。
- 内嵌 Chromium 中模型就绪，检查时无 error 级别日志。验证高精度网格详情和第三通道、坐标 `(511, 0)`、第 15 轮 `46.049 dB`、16× 局部放大与绝对误差图。
- 英文九个网络节点与五种存储详情均无残留中文。原图对比切换及滑块 0% / 100% 正常；中英文语言切换保留 `#architecture`。
- 视觉检查中文桌面结构、英文 320 px 结构与比较视图、中文 390 px 存储视图；所测窄屏无页面横向溢出。
- 网页中的 ShaderToy 链接标注旧版示例；本轮只更新本地展示工程，未更新线上 ShaderToy 模型，也没有宣称全图浏览器推理或新速度基准。

## 初版页面（2026-09-19）

2026-09-19，本地静态服务器与 Chromium 内嵌浏览器。

- `node verify.mjs` 通过：8 个导入资产的 SHA-256、模型 259,912 字节、含描述 261,526 字节、实际质量指标、解码层形状和双线性权重。
- 12 个 PyTorch 参考像素（含四角）通过：最大 RGB 浮点绝对误差 `1.7385075645748316e-7`，低于 `1e-4` 容差。
- `node --check app.mjs` 通过。
- 页面启动完成，浏览器未发现 error 级别日志。
- 原图 / BC7 切换、比较滑块首尾、预设像素与边界坐标 (511, 0)、粗网格插值详情、第三隐藏层 80 个激活均验证通过。
- 粗网格第五通道切换、搜索曲线第 12 轮 43.636 dB、16× 放大、统一绝对误差图均验证通过。
- 桌面默认宽度、390 px 与 320 px 视口检查；两种手机宽度均无页面横向溢出。
- 图像素材、模型、脚本、样式和字体均不需要外部网络资源。

网页实时运行的是单像素 JavaScript 解码器。全图指标基于已保存并验证的 RGB8 PNG；未在此页运行全图 GPU 推理或重做速度基准。

## 英文默认页与中文切换

- `/`、`/index.html` 默认英文；`/index-CN.html` 为中文，两页顶部均有相互切换的链接。
- 英文页面静态内容检查：除中文语言切换入口外，无未翻译中文。
- 浏览器中逐个检查八个网络节点、粗网格第五通道、曲线末轮、误差图：动态说明均为英文。
- 已验证从英文页切到中文页保留 `#architecture`，中文模型加载及动态说明正常，返回英文链接指向 `index.html#architecture`。
- 英文桌面首屏已视觉检查。后续浏览器工具发生超时，本轮未完成手机视口复查；此前中文页面的窄屏验证见上文。

## 页面底部精简（2026-09-20）

- 中英文页面移除实验资料整块内容及所有报告、模型下载入口，底部改为左侧区域结论、右侧误差表；手机端沿用上下排列。
- 删除网页目录中的两份报告副本和对应素材导入、翻译、资源清单项。
- 模型二进制与解码描述继续用于页面实时交互，不作为下载入口展示。
- 重新生成两种语言和素材清单，6 个运行资产哈希与 12 个参考像素校验通过，最大 RGB 浮点差仍为 `1.7385075645748316e-7`。
